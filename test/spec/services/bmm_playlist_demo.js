'use strict';

describe('Service: bmmPlaylistDemo', function () {

  // load the service's module
  beforeEach(module('bmmLibApp'));

  // instantiate service
  var bmmPlaylistDemo;
  beforeEach(inject(function (_bmmPlaylistDemo_) {
    bmmPlaylistDemo = _bmmPlaylistDemo_;
  }));

  it('should do something', function () {
    //expect(!!bmmPlaylistDemo).toBe(true);
  });

});
